import 'package:flutter/material.dart';
import 'package:giphy/ui/home_screen.dart';

void main() => runApp(MaterialApp(
      home: HomeScreen(),
      theme: ThemeData(hintColor: Colors.white),
    ));
